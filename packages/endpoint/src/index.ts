//import Endpoint from "./endpoint";
export * from "./socket";
export { default } from "axios";
export { default as Endpoint } from "./endpoint";

//export default new Endpoint({ base: "/api", socket: "/socket" } as any);
//export default axios.create({ baseURL: `${window.location.origin}/api` });
